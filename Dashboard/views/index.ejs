<!DOCTYPE html>
  <html>
    <head>
        <meta charset="utf-8">
        <title>Index</title>
            <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">     

       <!-- my own css file link in the last position to be priority-->
       <link rel="stylesheet" type="text/css" href="/stylesheets/index.css"> 
      <!-- Put the static files in the static folder inside your app.  
           Django collects files within all static folders and puts them within a single folder -->    
    </head>
    <body>
      <h3>All the Mongooses of our beautiful Zoo</h3>
      <table class="table table-striped w-800">
        <thead>
          <tr>
            <th scope="col">Name</th>
            <th scope="col">Type</th>
            <th scope="col">Age</th>
            <th scope="col">Status</th>
            <th scope="col">More details?</th>
          </tr>
        </thead>
        <tbody>
          <% for(var i of result){ %>
            <tr>
              <td><%= i.name %></td>
              <td><%= i.type %></td>
              <td><%= i.age %></td>
              <td><%= i.status %></td>
              <td><a href="/mongooses/<%= i._id %>" class="btn btn-primary">More Details</a><a href="mongooses/edit/<%= i._id %>" class="btn btn-primary">Edit</a><a href="/mongooses/destroy/<%= i._id %>" class="btn btn-primary">Delete</a></td>
            </tr>
          <% } %> 
        </tbody>
      </table>
      <a href="/mongooses/new" class="btn btn-primary">Add a new mongoose</a>
    </body>
  </html>
